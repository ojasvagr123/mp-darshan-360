{
  "name": "mp-darshan-360",
  "version": "1.0.0",
  "private": true,
  "description": "MP hidden tourism 360 panorama full-stack application",
  "scripts": {
    "install:all": "npm install --prefix apps/backend && npm install --prefix apps/frontend",
    "dev": "concurrently \"npm run dev --prefix apps/backend\" \"npm run dev --prefix apps/frontend\"",
    "build": "npm run build --prefix apps/frontend",
    "start": "npm run start --prefix apps/backend",
    "db:generate": "npm run db:generate --prefix apps/backend",
    "db:push": "npm run db:push --prefix apps/backend",
    "db:seed": "npm run db:seed --prefix apps/backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
